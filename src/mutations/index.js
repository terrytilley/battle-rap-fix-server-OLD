import { signup, editUser, deleteUser } from './user';
import { createPost, editPost, deletePost } from './post';

export default {
  signup,
  createPost,
  editUser,
  editPost,
  deleteUser,
  deletePost,
};
