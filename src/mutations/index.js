import { createUser, editUser, deleteUser } from './user';
import { createPost, editPost, deletePost } from './post';

export default {
  createUser,
  createPost,
  editUser,
  editPost,
  deleteUser,
  deletePost,
};
