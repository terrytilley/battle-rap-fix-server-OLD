import { signup, login, editUser, deleteUser } from './user';
import { createPost, editPost, deletePost } from './post';

export default {
  signup,
  login,
  createPost,
  editUser,
  editPost,
  deleteUser,
  deletePost,
};
